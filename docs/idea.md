the app when started should verify if all tools required are available and the UI should be able to block or further
interactions in case the minimal tools are not present

filesystem mode: git, claude are mandatory binaries
docker mode: devcontainer, and podman|docker are mandatory
kubernetes mode: unsure yet, but writing here just to keep in mind