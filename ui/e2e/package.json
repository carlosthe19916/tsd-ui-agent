{
  "name": "@tsd-ui/e2e",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./test-results ./playwright-report",
    "clean:all": "rimraf ./dist ./node_modules",
    "codegen": "npx playwright codegen http://localhost:3000/",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --project='chromium'",
    "test:ui:host": "npx playwright test --ui-host 127.0.0.1",
    "lint": "biome check --error-on-warnings . && npx tsc --noEmit",
    "lint:fix": "biome check --write .",
    "format": "biome format .",
    "format:fix": "biome format --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
